name: List
description: Use when displaying a stacked vertical collection of similar items — a fill-width container that holds multiple list items.
structure:
  type: FRAME
  layoutMode: VERTICAL
  children: []
defaultProps:
  layoutSizingHorizontal: FILL
  semantic: LIST
