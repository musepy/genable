name: Radio
description: Use when offering a single-choice selection from a group of mutually exclusive options — composed of a circle marker and adjacent label.
structure:
  type: FRAME
  layoutMode: HORIZONTAL
  children:
    - radio-circle
    - radio-label
defaultProps:
  counterAxisAlignItems: CENTER
  semantic: RADIO
