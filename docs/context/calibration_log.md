# 校准日志 (Calibration Log - Self-Correction)
> **用途**: 定期自我批评，以提高计划准确性和 Agent 行为质量。
> **频率**: 在每个主要阶段或重大错误后更新。

## 📉 校准事件 (Calibration Events)

### [2026-01-20] Phase 3: 配置层收敛
- **计划**: 移除全局约束并重构消费者。
- **实际**: 成功移除了 `semantic-constraints.json`。但最初忽略了 `errors.ts` 中对 `DSL_START` 常量的引用，导致构建失败。
- **偏差**: 在静态分析阶段发现了问题，未造成运行时错误，但显示了依赖检查的不完整。
- **教训**: 将“常量”更改为“动态加载器”时，必须进行全量的由外而内的“查找引用”检查，即使在类型定义文件中也是如此。
- **行动**: 在标记任务完成之前，将 `npm run build` 添加为强制性验证步骤。

## 🎯 准确度追踪 (Accuracy Tracking)
| 日期 | 任务 | 预估复杂度 | 实际复杂度 | 准确度 | 备注 |
|------|------|------------|------------|--------|------|
| 01-20 | 重构 designPolicy | 中 (5 工具调用) | 中 (6 工具调用) | 高 | 漏掉了一个文件 (`errors.ts`) |

## 🧘 自我批评 / 启发式 (Self-Criticism)
- **假设检查**: 是否因为文件在 `config/` 目录下就假设它未被使用？ -> *在删除前永远先 Grep。*
- **系统 2 思维**: 定义动态 Regex 时，确保 JSON 字符串格式中的双重转义是正确的。
