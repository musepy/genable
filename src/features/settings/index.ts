/**
 * @file index.ts
 * @description Settings feature public API
 * 
 * Re-exports the existing SettingsPanel component.
 * Future: Move SettingsPanel.tsx here and add useSettings hook.
 */

// Re-export existing component (will be migrated later)
export { SettingsPanel } from '../../ui/SettingsPanel';
