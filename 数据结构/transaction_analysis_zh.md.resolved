# 分析报告：Figma AI 智能体中的事务处理工具

这份报告分析了项目中类事务工具（Transaction-like tools）的用途、必要性，以及在“以工具驱动设计”模式下与数据库操作的类比。

## 1. 工具功能分析

| 工具 | 核心用途 | 现实世界类比 |
| :--- | :--- | :--- |
| **`beginTransaction`** | 标记一个逻辑工作单元的开始。在 Figma 中，这通常用于设置撤销历史（Undo History）的检查点。 | SQL 中的 `BEGIN` |
| **`commitTransaction`** | 提交所有挂起的更改。如果该轮次内的所有步骤都成功，则更改正式应用到画布。 | SQL 中的 `COMMIT` |
| **`rollbackTransaction`**| 回滚自上次 `beginTransaction` 以来所做的所有更改。用于在发生意外或验证失败时恢复画布状态。 | SQL 中的 `ROLLBACK` |
| **`batchOperations`** | 将多个原子操作（如创建节点、更新样式、设置布局）打包成一个请求。 | 数据库的批量插入（Bulk API） |

## 2. 必要性评估

### 为什么这些工具很重要：
1.  **原子性（Atomicity）**：如果没有事务，如果 Agent 在创建一个复杂组件（如带有图标和文字的按钮）时中途失败，画布上会留下“半成品”。事务能确保操作“要么全做，要么全不做”。
2.  **性能优化（批量化）**：目前每个工具调用都需要在 Sandbox（沙盒环境）和 Figma Main（主线程）之间进行一次 IPC 通信。`batchOperations` 可以实现一次往返完成多项任务，极大提升响应速度。
3.  **整洁的撤销历史**：通过将多次细微更改包装在一个事务中，可以避免用户的撤销列表（Cmd+Z）被几十个“修改属性”操作占满。

### 潜在挑战：
-   **状态管理复杂化**：要求 LLM 手动管理 `begin`/`commit` 状态可能会增加逻辑负担，一旦模型忘记关闭事务，可能会导致内存或逻辑锁定。
-   **失败归因难度**：回滚虽然保护了画布，但如果一个庞大的批处理（Batch）失败了，LLM 可能需要更精确的错误反馈才能知道是哪一步出了问题。

## 3. 数据库操作类比

**“项目的能力是否像在操作数据库？”**

**是的，非常有此趋势。** 项目已经演变成了一个针对 Figma 层级树（SceneGraph）的 **CRUD（增删改查）抽象层**。

-   **SceneGraph 即数据库**：Figma 文件就像一个持久化的、结构化的文档型数据库。
-   **节点即记录**：每个图层（Layer）都有唯一的 ID（主键），并由类型（FRAME, TEXT 等）定义其 Schema（结构）。
-   **工具即变更（Mutations）**：`createNode`、`setNodeStyles` 等工具本质上就是 `INSERT` 或 `UPDATE` 语句。
-   **缺失的 ACID 特性**：我们目前已有**一致性**（Figma API 规则约束）和**持久性**（Figma 云端保存）。我们目前缺失的是**原子性**和**隔离性**，而你提到的这些事务工具正是为了解决这两个问题。

## 4. 结论与建议

目前的 **原子工具流 (v1.2.0)** 是这套引擎的“汇编语言”。引入事务和批量处理工具是逻辑上的必然，它能将 Agent 的操作从“搬砖头”进化为“一键生成复杂结构”。

> [!TIP]
> 考虑到 LLM 的复杂性，与其让 LLM 显示地调用 `begin/commit`（容易遗漏），不如在 **AgentRuntime 内部实现自动事务包裹**：将 Agent 在 single iteration（单次迭代）中产生的所有操作自动打包成一个原子操作。
